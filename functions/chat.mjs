import OpenAI from "openai"; const client = new OpenAI({ apiKey: process.env.OPENAI_API_KEY }); export default async (req) => { if (req.method !== "POST") { return new Response("Method Not Allowed", { status: 405 }); } try { const { message } = await req.json(); if (!message || typeof message !== "string") { return new Response( JSON.stringify({ error: "Message is required." }), { status: 400, headers: { "Content-Type": "application/json" } } ); } const response = await client.responses.create({ model: "gpt-5.6-luna", instructions: "You are Ritu-Chakra AI. Help students understand Indian Knowledge Systems, Ritucharya, seasonal living, nutrition, and sustainability. Give clear, practical, student-friendly answers.", input: message }); return new Response( JSON.stringify({ answer: response.output_text }), { status: 200, headers: { "Content-Type": "application/json" } } ); } catch (error) { console.error(error); return new Response( JSON.stringify({ error: "Something went wrong with Ritu-Chakra AI." }), { status: 500, headers: { "Content-Type": "application/json" } } ); } };